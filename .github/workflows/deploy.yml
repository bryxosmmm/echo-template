name: Deploy on server

on:
  push:
    branches:
      - master  

jobs:
  run-docker-compose:
    runs-on: self-hosted
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Run server
        run: docker-compose up --build -d 


